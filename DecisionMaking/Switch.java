package DecisionMaking;

public class Switch {
    public static void main(String[] args) {
        // when we use equality replacemnets 
        // break:exit from block ie. switch block
        // can also accpet string values 
        // can use ->  in place of : and we don't need further break
        // case "burger","roll"-> 
        // can have multiple keys 
        // can also we stored in a varable 
        // can return values as functions with yield 20; 
        // return swich(item):
    }
}
